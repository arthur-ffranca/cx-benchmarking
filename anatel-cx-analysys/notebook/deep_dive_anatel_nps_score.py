# -*- coding: utf-8 -*-
"""Deep Dive Anatel NPS Score

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1b7FX0qZHYl2MXOS4covR0m6-A0iI3YlH
"""

from google.colab import drive
drive.mount('/content/drive')

"""As análises desenvolvidas foram complementadas com referências públicas da Pesquisa de Satisfação e Qualidade Percebida da Anatel, utilizada como benchmark para contextualização dos resultados observados.
Através do link: https://dados.gov.br/dados/conjuntos-dados/pesquisas-de-satisfacao-e-qualidade-percebida

"""

import pandas as pd

# Assuming the file path is the same as the previously loaded file
# If your file name has changed, please update 'base_anatel_nps.xlsx' accordingly.
file_path = '/content/drive/MyDrive/base_anatel_nps.xlsx'

# Re-read the Excel file to get the latest changes
df = pd.read_excel(file_path)

# Display the head of the updated DataFrame to verify changes
print(df.head())

df

"""# Trabalhando com SQL - DuckDB
Importando a biblioteca 'duckdb' para poder trabalhar com a linguagem SQL para o tratamento dos dados e criar um workflow pipeline.
"""

import duckdb

duckdb.register("nps", df)

"""# Indústria



*   Criando a Regra Padrão para calcular a quantidade de Promotores, Neutros e Detratores que será a mesma para as proximas análises
*   Definindo os Promotores e Detratores, foi desenvolvido uma subquery para calcular o NPS por Indústria


"""

nps_industria = duckdb.sql("""
SELECT
  industria,
  COUNT(*) AS respostas,
  AVG(nps_score) AS media_nps,
  SUM(CASE WHEN nps_score >= 9 THEN 1 ELSE 0 END) AS Promotores,
  SUM(CASE WHEN nps_score BETWEEN 7 AND 8 THEN 1 ELSE 0 END) AS Neutros,  --- Definindo métricas para identificar Promotores, Neutros e Detratores
  SUM(CASE WHEN nps_score <= 6 THEN 1 ELSE 0 END) AS Detratores,
  (
    SUM(CASE WHEN nps_score >= 9 THEN 1 ELSE 0 END) * 100.0 / COUNT(*)
    -
    SUM(CASE WHEN nps_score <= 6 THEN 1 ELSE 0 END) * 100.0 / COUNT(*)  --- Através de uma Subquery, foi possível calcular O valor do NPS
  ) AS NPS

FROM NPS
GROUP BY industria
ORDER BY NPS DESC;

""").df()

nps_industria

"""# Principais Insights — NPS por Indústria



*   Todos os segmentos analisados apresentam NPS negativo, indicando oportunidades relevantes de melhoria na experiência do cliente.

*   Seguros apresenta a melhor performance relativa do estudo, sustentada por maior participação de promotores e menor pressão de detratores.
*   Telecom concentra o pior resultado de NPS, sugerindo maior fricção na jornada do cliente e maior risco de insatisfação e churn.

# EMPRESA

*   As Métricas para calcular Promotores, Neutros e Detratores seguem o mesmo padrão
*  E definindo a coluna 'Empresa' como base de performance
"""

nps_empresa = duckdb.sql("""
SELECT
  empresa,
  COUNT(*) AS respostas,
  AVG(nps_score) AS media_nps,
  SUM(CASE WHEN nps_score >= 9 THEN 1 ELSE 0 END) AS Promotores,
  SUM(CASE WHEN nps_score BETWEEN 7 AND 8 THEN 1 ELSE 0 END) AS Neutros,
  SUM(CASE WHEN nps_score <= 6 THEN 1 ELSE 0 END) AS Detratores,
  (
    SUM(CASE WHEN nps_score >= 9 THEN 1 ELSE 0 END) * 100.0 / COUNT(*)
    -
    SUM(CASE WHEN nps_score <= 6 THEN 1 ELSE 0 END) * 100.0 / COUNT(*)
  ) AS NPS

FROM NPS
GROUP BY empresa
ORDER BY NPS DESC;

""").df()

nps_empresa

"""# NPS por Empresa

A análise por empresa revela forte dispersão de performance dentro da mesma base. Nexa Bank é a única empresa com NPS positivo, ainda que baixo, com +1,64, indicando equilíbrio quase neutro entre promotores e detratores.

Na ponta negativa, CompreJá e TradBank apresentam os piores resultados, ambas com NPS de -64,81, impulsionadas pela alta concentração de detratores e baixíssima geração de promotores.

Um ponto importante é que a média da nota sozinha não conta toda a história: empresas como LegacyTel e LojaFácil têm médias próximas de 6, mas NPS muito negativos, porque poucos clientes chegam à faixa de promotores.
"""

nps_jornada = duckdb.sql("""
SELECT
  jornada,
  COUNT(*) AS respostas,
  AVG(nps_score) AS media_nps,
  SUM(CASE WHEN nps_score >= 9 THEN 1 ELSE 0 END) AS Promotores,
  SUM(CASE WHEN nps_score BETWEEN 7 AND 8 THEN 1 ELSE 0 END) AS Neutros,
  SUM(CASE WHEN nps_score <= 6 THEN 1 ELSE 0 END) AS Detratores,
  (
    SUM(CASE WHEN nps_score >= 9 THEN 1 ELSE 0 END) * 100.0 / COUNT(*)
    -
    SUM(CASE WHEN nps_score <= 6 THEN 1 ELSE 0 END) * 100.0 / COUNT(*)
  ) AS NPS

FROM NPS
GROUP BY jornada
ORDER BY NPS DESC;

""").df()

nps_jornada

"""# Principais Insights - NPS por Jornada

A análise por jornada indica que a insatisfação está concentrada em momentos de maior fricção operacional, especialmente cancelamento, reclamação e atendimento. Isso sugere que a experiência pós-venda e de resolução de problemas é o principal driver negativo do NPS.
"""

nps_canal = duckdb.sql("""
SELECT
  canal,
  COUNT(*) AS respostas,
  AVG(nps_score) AS media_nps,
  SUM(CASE WHEN nps_score >= 9 THEN 1 ELSE 0 END) AS Promotores,
  SUM(CASE WHEN nps_score BETWEEN 7 AND 8 THEN 1 ELSE 0 END) AS Neutros,
  SUM(CASE WHEN nps_score <= 6 THEN 1 ELSE 0 END) AS Detratores,
  (
    SUM(CASE WHEN nps_score >= 9 THEN 1 ELSE 0 END) * 100.0 / COUNT(*)
    -
    SUM(CASE WHEN nps_score <= 6 THEN 1 ELSE 0 END) * 100.0 / COUNT(*)
  ) AS NPS

FROM NPS
GROUP BY canal
ORDER BY NPS DESC;

""").df()

nps_canal

"""# Principais Insights - NPS por Canal

A análise por canal sugere que os canais digitais/autônomos, especialmente Chatbot, apresentam maior fricção na experiência do cliente. Já Loja/Agência tem a melhor performance relativa, indicando que interações presenciais ou assistidas podem estar mitigando parte da insatisfação.

# NPS - Jornada x Canal
"""

nps_jornada_canal = duckdb.sql("""
SELECT
  jornada,
  canal,
  COUNT(*) AS respostas,
  AVG(nps_score) AS media_nps,
  SUM(CASE WHEN nps_score >= 9 THEN 1 ELSE 0 END) AS Promotores,
  SUM(CASE WHEN nps_score BETWEEN 7 AND 8 THEN 1 ELSE 0 END) AS Neutros,
  SUM(CASE WHEN nps_score <= 6 THEN 1 ELSE 0 END) AS Detratores,
  (
    SUM(CASE WHEN nps_score >= 9 THEN 1 ELSE 0 END) * 100.0 / COUNT(*)
    -
    SUM(CASE WHEN nps_score <= 6 THEN 1 ELSE 0 END) * 100.0 / COUNT(*)
  ) AS NPS

FROM NPS
GROUP BY jornada, canal
ORDER BY NPS ASC;

""").df()

nps_jornada_canal

"""# Insights Jornada x Canal
A prioridade não deve ser melhorar todos os canais de forma genérica, mas atacar combinações específicas de alto impacto: cancelamento no call center, reclamação via chatbot e atendimento via chatbot, que correspondem canais com menor capacidade de resolução rápida para o cliente.

# NPS - Produto x Segmento
"""

nps_produto_segmento = duckdb.sql("""
SELECT
  produto,
  segmento,
  COUNT(*) AS respostas,
  AVG(nps_score) AS media_nps,
  SUM(CASE WHEN nps_score >= 9 THEN 1 ELSE 0 END) AS Promotores,
  SUM(CASE WHEN nps_score BETWEEN 7 AND 8 THEN 1 ELSE 0 END) AS Neutros,
  SUM(CASE WHEN nps_score <= 6 THEN 1 ELSE 0 END) AS Detratores,
  (
    SUM(CASE WHEN nps_score >= 9 THEN 1 ELSE 0 END) * 100.0 / COUNT(*)
    -
    SUM(CASE WHEN nps_score <= 6 THEN 1 ELSE 0 END) * 100.0 / COUNT(*)
  ) AS NPS

FROM NPS
GROUP BY produto, segmento
ORDER BY NPS ASC;

""").df()

nps_produto_segmento

"""# Insights - Produto x Segmento

O cruzamento Produto × Segmento indica que a insatisfação está concentrada em combinações específicas de oferta e perfil de cliente. Celular Pós-Pago para Baixa Renda, Loja Física para PME e Empréstimo para Média Renda aparecem como principais pontos críticos, sugerindo necessidade de ações segmentadas e não apenas melhorias genéricas por produto

Loja Física aparece negativamente em diferentes segmentos, o que sugere um problema mais estrutural do produto/canal físico.

O Celular Pós-Pago é especialmente crítico para Baixa Renda, apontando possível desalinhamento entre oferta, preço, atendimento ou expectativa desse público

# NPS - Segmento x Jornada
"""

nps_segmento_jornada = duckdb.sql("""
SELECT
  segmento,
  jornada,
  COUNT(*) AS respostas,
  AVG(nps_score) AS media_nps,
  SUM(CASE WHEN nps_score >= 9 THEN 1 ELSE 0 END) AS Promotores,
  SUM(CASE WHEN nps_score BETWEEN 7 AND 8 THEN 1 ELSE 0 END) AS Neutros,
  SUM(CASE WHEN nps_score <= 6 THEN 1 ELSE 0 END) AS Detratores,
  (
    SUM(CASE WHEN nps_score >= 9 THEN 1 ELSE 0 END) * 100.0 / COUNT(*)
    -
    SUM(CASE WHEN nps_score <= 6 THEN 1 ELSE 0 END) * 100.0 / COUNT(*)
  ) AS NPS

FROM NPS
GROUP BY segmento, jornada
ORDER BY NPS ASC;

""").df()

nps_segmento_jornada

"""# Insights Segmento x Jornada

* As ações de melhoria devem ser segmentadas: PME exige reforço em gestão de reclamações e retenção;
* Baixa Renda precisa de uma jornada de cancelamento mais clara e assistida;
* Premium parece responder melhor às etapas digitais e iniciais da jornada.
"""

import pandas as pd

with pd.ExcelWriter("dashboard_nps.xlsx", engine="openpyxl") as writer:

    nps_industria.to_excel(writer, sheet_name="Industria", index=False)
    nps_empresa.to_excel(writer, sheet_name="Empresa", index=False)
    nps_jornada.to_excel(writer, sheet_name="Jornada", index=False)
    nps_canal.to_excel(writer, sheet_name="Canal", index=False)
    nps_jornada_canal.to_excel(writer, sheet_name="Jornada_Canal", index=False)
    nps_segmento_jornada.to_excel(writer, sheet_name="Segmento_Jornada", index=False)
    nps_produto_segmento.to_excel(writer, sheet_name="Produto_Segmento", index=False)
print("Arquivo criado")